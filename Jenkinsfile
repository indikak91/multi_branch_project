pipeline{
    agent any
    stages {
        stage ('Production Branch') {
            steps{
                checkout scm
                sh 'echo production branch'
            }
        }
    }  
}
